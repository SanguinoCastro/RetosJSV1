//Usando el bucle for, añadimos todos los parametros en la misma orden, se leeria de esta manera: para el valor "i" que vale 1, si es mayor o igual a 100 se ejecutara lo que hat entre {} y luego se le sumará 1 a su valor y se repetirá el bucle hasta que el valor "i" sea mayor que 100 que entonces ya no se imprimirá ni se añadirá mas valor y el bucle finalizará. 
for (i = 1; i <= 100; i++){
    console.log(i);
}

//Como se puede comprobar al usar el bucle for el código queda mas limpio y reducido.